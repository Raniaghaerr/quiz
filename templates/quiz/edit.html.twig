{% extends 'base.html.twig' %}

{% block title %}Edit Quiz{% endblock %}

{% block body %}
    <h1>Edit Quiz</h1>

    {{ form_start(form) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.nbrequestion) }}
        {{ form_row(form.duree) }}
        <button class="btn">Update</button>
    {{ form_end(form) }}

    <a href="{{ path('quiz_index') }}">Back to list</a>
{% endblock %}
