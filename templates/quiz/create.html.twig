{% extends 'base.html.twig' %}

{% block title %}Create Quiz{% endblock %}

{% block body %}
    <h1>Create a new Quiz</h1>

    {{ form_start(form) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.nbQuestions) }}
        {{ form_row(form.duree) }}
        <button class="btn">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('quiz_index') }}">Back to list</a>
{% endblock %}
